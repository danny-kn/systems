#include <stdio.h>
#include <stdlib.h>
#include "bit_encode.h"

unsigned char cleartext[MAX_CHAR_LEN + 1] = {0};
unsigned char ciphertext[MAX_CHAR_LEN + 1] = {0};

int main(void) {
  return EXIT_SUCCESS;
}
